# MCP Server Configuration

# Transport mode: stdio or sse
MCP_TRANSPORT=sse

# SSE server host (use 0.0.0.0 for Docker)
MCP_HOST=0.0.0.0

# SSE server port
MCP_PORT=8000

# Vault path (inside container)
# VAULT_PATH=/app/vault
